#include "inc/stdafx.hpp"
#include <future>
#include <chrono>
#include <algorithm>
#include <unordered_map>

/**
 * @brief main The main entry of program
 * @return The exit flag
 */
int main(int, char**) {
    exit(EXIT_SUCCESS);
}
